{
  "name": "supply-chain-tracker-client",
  "version": "1.0.0",
  "description": "React frontend for decentralized supply chain tracking",
  "private": true,
  "scripts": {
    "start": "serve -s . -l 
3000",
    "build": "echo 'Static files ready'",
    "test": "echo 'No tests specified'",
    "dev": "python -m http.server 3000 || python3 -m http.server 3000"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
